<%- include("partials/header.ejs") %> 
<main id="booklist">
    <header>
      <h1>Books I’ve read</h1>
      <p>Tiny summary but <strong>detailed notes for each</strong>. Use the ISBN number to find it from your local library or anywhere else. <strong>This page will constantly update as I read more</strong>, so bookmark it if you want to check back in a few months.</p>
    </header>
    <p id="sorters">Sorted with <strong>my top recommendations up top</strong>. Sort by
      <a href="/book?sort=title" id="sort-title">title</a>,
      <a href="/book?sort=date" id="sort-date">newest</a>, or
      <a href="/book?sort=score" id="sort-rating">best</a>.
    </p><hr>
    
    <section id="allbooks">
    <% books.forEach(book => { %>

    <div class="abook">
    <figure><a href="/book/<%= book.id %>"><img src="https://covers.openlibrary.org/b/isbn/<%= book.isbn %>-M.jpg" style="width: 125px; height: 187px;" loading="fast" alt="<%= book.title %> - by <%= book.author %>"></a></figure>
    <h2><a href="/book/<%= book.id %>"><%= book.title %> - by <%= book.author %></a></h2>
    <small>Date read: <%= book.date.toISOString().split('T')[0] %>. How strongly I recommend it: <strong><%= book.score %></strong>/10</small>
    <p><%= book.summary %></p>
    <p><strong><a href="/book/<%= book.id %>">Read my notes</a></strong>, or go to the <a href=<%= book.amazon %>>Amazon page</a> for details and reviews.
    </p></div>
    
    <% });  %>
    </section>  
    
  </main>

<%- include("partials/footer.ejs") %>
